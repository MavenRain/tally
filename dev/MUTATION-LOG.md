# M0 mutation log

One row per mutation cycle: mutate, observe red, restore, re-run, observe
green.  A gate with no row here is not accepted (M0-PLAN.md section 5).

| stage | gate | mutation | red (observed) | restore | green (observed) |
|---|---|---|---|---|---|
| A | PASS-T0-HARNESS (timing) | wrapper delay: candidate binary replaced by `/bin/sleep 0.05; exec "$REAL" "$@"` | the 2x comparator leg `test "$CANDM" -le $(( BASEM * 2 ))` exits 1: `BASEM=17478 CANDM=110778 limit=34956` | drop the wrapper, re-invoke the harness with the true binary as candidate | harness exit 0, `BASEM=24179 CANDM=17036`, comparator leg exit 0 |
| A | PASS-T0-HARNESS (timing) | nonexistent binary: CAND_BIN pointed at `/Users/oobi/Documents/tally/no/such/binary.exe` | harness exit 3, `timing_harness2: missing or non-executable CAND_BIN: ...`, and NO `cand-warm-median.txt` written (no microsecond "fast" number) | re-invoke with the real binary | harness exit 0, OUTDIR populated |
| A | PASS-T0-HARNESS (timing) | non-zero check exit: CORPUS pointed at a file the binary rejects | harness exit 3, `timing_harness2: check exited 1 ... unknown name Nope` | re-invoke on `dev/corpus/corpus-a.tot` | harness exit 0 |
| A | PASS-T0-HARNESS (timing) | missing prelude argument: BASE_PRELUDE pointed at `/Users/oobi/Documents/tally/no/such/prelude.tot` | harness exit 3, `timing_harness2: missing BASE_PRELUDE: ...` (the R7 contract row) | re-invoke with the vendor tree's prelude | harness exit 0 |
| A | PASS-T0-HARNESS (negcheck) | wrong-cwd: the two `negcheck` capture legs re-run from a scratch directory instead of the tree root | the `exit` file still records 1 (exit leg green, as the plan predicts) while the stderr leg `rg -q 'duplicate global Nat'` exits 1 on `prelude: .../wrongcwd/stdlib/prelude.tot: no such file`, the R7 cwd-anchored `TOT_PRELUDE` spelling | re-run the same legs from `/Users/oobi/Documents/tally/vendor/tot` | exit leg 0 and stderr leg 0 on `examples/nat.tot:2:1: duplicate global Nat` |
| A | PASS-T0-HARNESS (determinism) | run 2: one byte `X` appended to `$OUT/dryrun-base2/040476262abc771cf0839efb2a2c0dd6/stdout` (the `check test/fixtures/m4b-deceq-runs.tot` capture, 33 -> 34 bytes) | the leg ABORTS, script exit 1, no `PASS-T0-HARNESS-DETERMINISM` line: `diff -r ... 2a3 > X \ No newline at end of file` then `test "$hdst" -eq 0` red | `cp "$OUT/dryrun-base1/040476262abc771cf0839efb2a2c0dd6/stdout" "$OUT/dryrun-base2/040476262abc771cf0839efb2a2c0dd6/stdout"` (the untouched base1 twin) | 33 bytes again, leg exit 0, `PASS-T0-HARNESS-DETERMINISM` |
| A | PASS-T0-HARNESS (corpus freeze) | run 2: last row `test/fixtures/x9-proc-noexec.tot` deleted from `dev/corpus-files.txt` (66 -> 65 rows), then ONLY the two `run-matrix.sh` lines and the row-count leg re-run, never the `ls-files` derivation | `CORPUS-ROWS=65`, `ROW-COUNT-OBSERVED=261` against the frozen 265, `test "$(ls "$D" \| wc -l)" -eq 265` red, script exit 1, no `ROW-COUNT-LEG-GREEN` | `git -C /Users/oobi/Documents/tally checkout -- dev/corpus-files.txt` (reads the index the PASS-T0-HARNESS staging line holds at 66 rows) | `CORPUS-ROWS=66`, `ROW-COUNT-OBSERVED=265`, `ROW-COUNT-LEG-GREEN`, exit 0 |
| A | PASS-T0-HARNESS (floor script) | run 2: `dev/gates-stage-a.sh` cut to its two-line `#!/bin/zsh` + `set -e` prologue | call site reports `FL=0 EXIT-LEG=0 MARKER-LEG=1` with `floors-a.txt: []`: the exit-status leg alone stays GREEN (the F96 hole) while `rg -qx 'PASS-T0-HARNESS-FLOORS'` is red | `git -C /Users/oobi/Documents/tally checkout -- dev/gates-stage-a.sh` | 11 lines restored, `FL=0 EXIT-LEG=0 MARKER-LEG=0`, `floors-a.txt: [PASS-T0-HARNESS-FLOORS]` |
| A | PASS-T0-HARNESS (D8 watchdog allowlist) | M-ALLOW-1 allowlist-drive, three observations on ONE cell (`run test/fixtures/c-regex-pathological.tot`, re-run through a one-cell `run_one` runner with `OUT=/Users/oobi/Documents/tally-m0/scratch/stage-a-fix-mut`, tag `mutscratch`, cache under the same scratch, cwd `vendor/tot`): (i) `dev/watchdog-allowlist.txt` moved out of the tree, (ii) restored EMPTY | (i) `run_one` returns `ONECELL-RC=2` on `rg: ...watchdog-allowlist.txt: IO error ... No such file or directory (os error 2)` (fail-closed on a missing allowlist);  (ii) `ONECELL-RC=1` (rg no-match), `EXITFILE=124` both times | `git -C /Users/oobi/Documents/tally checkout -- dev/watchdog-allowlist.txt` (index holds the real one-row body) | `ONECELL-RC=0`, `ONECELL-ARGV=[run test/fixtures/c-regex-pathological.tot] EXITFILE=124 STDOUT=0 STDERR=0` |
| A | PASS-T0-HARNESS (D8 watchdog allowlist) | M-ALLOW-2 wedge-abort-alive: real allowlist in place, the same one-cell runner given a wrapper binary `slow.sh` (`#!/bin/zsh` + `/bin/sleep 60`) and the NON-allowlisted argv `check examples/nat.tot` | the watchdog fires (`EXITFILE=124`) and `run_one` returns `ONECELL-RC=1`: a timeout on any cell outside the one allowlist row still aborts the fail-fast matrix | n/a (the mutation is the wrapper binary and the scratch OUT dir, both deleted;  no tracked file was touched) | the same runner on the real binary and the allowlisted argv returns `ONECELL-RC=0` (row above), and the full re-run of both 265-capture cuts is green |
| B | step 7 baseline block (base-dead) | `BASE` rebound to `$BASEDIR/nonexistent.exe` and the whole step-7 block re-run (both cuts armed, `DOCUT=1`) | script exit 1 at the `test -x "$BASE"` opener BEFORE a single capture is cut: the run prints only `BASE=.../nonexistent.exe DOCUT=1` and never `LEG test-x: green`, so neither `run-matrix.sh` line ran | rebind `BASE` to the step-5 spelling `$BASEDIR/tot-baseline.exe` and re-run the block with both cuts | exit 0, both cuts re-made at 405 dirs each, `PASS-T0-BASELINE-ALIVE` + `PASS-T0-BASELINE-PRELUDE-RESOLVED` + `PASS-T0-BASELINE-DETERMINISM` (`lst=1`, `zero-exits=130`, `nfst=1`, `bdst=0`) |
| B | step 7 baseline block (base-dead, floor half) | probe-proved, never live-mutated (R9, F89): `scratch/r9f/f89/sensor.sh` re-run 2026-09-03 over a `run_one`-shaped healthy set and a dead-launcher set (every `exit` file `127`, gtimeout's capital-N stderr) | on the DEAD set the pre-R9 sensors score GREEN (`OLD-PRELUDE-nfst=1`, `OLD-DET-bdst=0`) while the new floors redden: `NEW-127-lst=0` (needs 1), `NEW-ZEROEXIT-count=EMPTY` (needs >= 40), `NEW-NAT-st=1` (needs 0);  live launcher evidence `GTIMEOUT-DEAD-EXIT=127` + `GT-STDERR-CAPITAL-N`;  opener polarity `TESTX-MISSING=1` | n/a (probe-only: the probe builds and deletes its own capture sets;  no tracked file and no reference cut was touched) | on the HEALTHY set every new leg is green (`NEW-127-lst=1`, `NEW-ZEROEXIT-count=132`, `NEW-NAT-st=0`, `NEW-PRELUDE-qi-nfst=1`), `TESTX-LIVE=0`, probe exit 0 |
| B | step 7 baseline block (det) | one byte `X` appended to `$OUT/base2/040476262abc771cf0839efb2a2c0dd6/stdout` (the `check test/fixtures/m4b-deceq-runs.tot` capture, 33 -> 34 bytes) | the `bdst` leg ABORTS the block, exit 1, no `PASS-T0-BASELINE-DETERMINISM` line: `diff -r .../base1/040476.../stdout .../base2/040476.../stdout  2a3 > X \ No newline at end of file` then `bdst=1`, `test "$bdst" -eq 0` red | `cp "$OUT/base1/040476262abc771cf0839efb2a2c0dd6/stdout" "$OUT/base2/040476262abc771cf0839efb2a2c0dd6/stdout"` (the untouched `base1` twin) | 33 bytes again, `bdst=0`, exit 0, `PASS-T0-BASELINE-DETERMINISM` |
| B | step 7 baseline block (prel) | one line `prelude: /nonexistent/prelude.tot: no such file` appended to `$OUT/base1/007e8eac9d19cf4aff637f70a9a05b0b/stderr` (the `check test/fixtures/m4d-serror-exit.tot` capture, 56 -> 104 bytes) | the `nfst` leg reddens at its OWN leg, BEFORE the determinism diff sees the same edit: `PASS-T0-BASELINE-ALIVE` printed, then `nfst=0` (rg exit 0 against the required 1), `test "$nfst" -eq 1` red, exit 1, no `PASS-T0-BASELINE-PRELUDE-RESOLVED` and no determinism line | `cp "$OUT/base2/007e8eac9d19cf4aff637f70a9a05b0b/stderr" "$OUT/base1/007e8eac9d19cf4aff637f70a9a05b0b/stderr"` (the untouched `base2` twin) | 56 bytes again, `nfst=1`, exit 0, `PASS-T0-BASELINE-PRELUDE-RESOLVED` and `PASS-T0-BASELINE-DETERMINISM` |
| B | PASS-T0-PIN (A) | `git -C vendor/tot checkout "$PINSHA"^`: the submodule one commit BEHIND `PIN` (HEAD `39234e484e04edbec98104b8f636559059ed38e9`) | leg 1 red, gate exit 1, empty stdout (no `PASS-T0-PIN`);  `zsh -x` tail: `git -C .../vendor/tot merge-base --is-ancestor 66b444fe8380c82f0a74093ffaa779371d014a9b HEAD` | `git -C vendor/tot checkout "$PINSHA"` (the recorded commit) | vendor HEAD `66b444fe...`, gate exit 0, `PASS-T0-PIN` and `PASS-T0-BASELINE` |
| B | PASS-T0-PIN (B) | one scratch commit made INSIDE `vendor/tot` (HEAD `bab4d0b9fa441940a36548fa03606cdba8903f83`, a descendant of `PIN`, so leg 1 stays green) with `git -C tally add vendor/tot` deliberately NOT re-run | leg 2 red, gate exit 1, empty stdout;  `zsh -x` tail: `test 66b444fe8380c82f0a74093ffaa779371d014a9b '=' bab4d0b9fa441940a36548fa03606cdba8903f83` (gitlink vs submodule HEAD) | `git -C vendor/tot checkout "$PINSHA"` (the scratch commit is abandoned), then `git -C vendor/tot add -A` and `git -C tally add vendor/tot` as the restore's named FINAL step (Stage B step 6 suspension exception;  R15, F122) | gitlink and vendor HEAD both `66b444fe...`, gate exit 0, `PASS-T0-PIN` |
| B | PASS-T0-PIN (C) | `$BASEDIR/pass-count.txt` truncated to ZERO bytes | the `test -s "$BASEDIR/pass-count.txt"` leg red, gate exit 1, empty stdout;  `zsh -x` tail shows the pin-record leg green immediately before it | rewrite the recorded count, `printf '371\n' > "$BASEDIR/pass-count.txt"` (`cmp` against the pre-mutation copy: identical) | gate exit 0, `PASS-T0-PIN` |
| B | PASS-T0-PIN (D) | `mkdir "$OUT/dryrun-base1"` after step 7's delete | the dryrun-absence leg red, gate exit 1, empty stdout;  `zsh -x` tail: `ls $OUT \| rg -q '^dryrun-'` then `test 0 -eq 1` | `rm -rf "$OUT/dryrun-base1"` | `$OUT` holds no `dryrun-*` child, gate exit 0, `PASS-T0-PIN` |
| B | PASS-T0-PIN (E) | `$BASEDIR/pin.txt` overwritten with the foreign SHA `0123456789abcdef0123456789abcdef01234567` | the pin-record leg red, gate exit 1, empty stdout;  `zsh -x` tail: `test 0123456789abcdef0123456789abcdef01234567 '=' 66b444fe8380c82f0a74093ffaa779371d014a9b` | rewrite the captured step-5 SHA, `printf '%s\n' "$PINSHA" > "$BASEDIR/pin.txt"` | gate exit 0, `PASS-T0-PIN` |
| B | PASS-T0-BASELINE (A) | `$BASEDIR/fail-count.txt` overwritten with `1` | the fail-count-zero leg red, gate exit 1;  `PASS-T0-PIN` still printed first, then `zsh -x` tail `test 1 -eq 0`;  no `PASS-T0-BASELINE` | restore the recorded `0`, `printf '0\n' > "$BASEDIR/fail-count.txt"` | gate exit 0, `PASS-T0-BASELINE` |
| B | PASS-T0-BASELINE (B) | `$BASEDIR/fail-count.txt` truncated to ZERO bytes (the exact shape the retired prose leg scored green under zsh's `test "" -eq 0`) | the `test -s "$BASEDIR/fail-count.txt"` leg red, gate exit 1;  `PASS-T0-PIN` printed, gate-exit legs green (`test 0 -eq 0`), no `PASS-T0-BASELINE` | restore the recorded `0` | gate exit 0, `PASS-T0-BASELINE` |
| B | PASS-T0-BASELINE (C) | `$BASEDIR/gate-exit.txt` overwritten with `1` | the gate-exit leg red, gate exit 1;  `PASS-T0-PIN` printed, `zsh -x` tail `test -s .../gate-exit.txt` then `test 1 -eq 0`;  no `PASS-T0-BASELINE` | restore the recorded `0`, `printf '0\n' > "$BASEDIR/gate-exit.txt"` | gate exit 0, `PASS-T0-BASELINE` |

## Frozen-literal arithmetic (the `NEW_FROZEN` freeze discipline)

Every frozen integer literal a gate compares against is derived ONCE here, with
its source rows named, and is then WRITTEN into the gate as a literal.  A gate
that re-derives its own expectation at gate time is not accepted (M0-PLAN.md
lines 1090-1098 and 2938-2951).

| stage | literal | site | arithmetic | value |
|---|---|---|---|---|
| A | `ROWS_FROZEN` | `dev/gates-stage-a.sh` | 66 corpus rows x 4 cells (`check`, `check --no-prelude`, `run`, `run --no-prelude`) + 1 `prims` row = 265 | 265 (the DRYRUN calibration, unchanged) |
| B | `ROWS_FROZEN` | `dev/gates-stage-c.sh` | rows of `dev/corpus-files.txt` at PIN_M5 = 101 (6 `examples/*.tot` + 95 `test/fixtures/*.tot`, from `git -C /Users/oobi/Documents/tally-m0-pin-m5 ls-files`) x 4 + 1 = 405;  both reference cuts measure `ls "$OUT/base1" \| wc -l` = 405 and `ls "$OUT/base2" \| wc -l` = 405 | 265 -> **405** |
| B | the four harness floors | `dev/gates-stage-c.sh` | CARRIED FORWARD unchanged from the Stage A DRYRUN calibration, never re-derived from `base1` (R9, F89: a born-degenerate cut must not calibrate its own floors).  Observed headroom at PIN_M5 over `base1`: 130 zero exits >= 40, 275 non-zero exits >= 20 | 40 / 20 / 20000 / 5000 (unchanged) |
| B | `AXIOM_ROWS_FROZEN` | battery entry 12 in `dev/gates-tally.sh` (still an `exit 9` placeholder at Stage B close) beside `dev/prelude-axioms.golden` | `rg -o '^axiom [A-Za-z0-9_]+' /Users/oobi/Documents/tally-m0-pin-m5/stdlib/prelude.tot` = 3 rows (`ioBindPure`, `ioBindRet`, `ioBindAssoc`);  `cmp` against `dev/prelude-axioms.golden` exits 0, so the golden is re-derived byte-identical and the count literal re-freezes from the golden's own row count, 3 | 3 (unchanged) |
